#include <stdio.h>

int main(void)
{
	int n;
	
	scanf("%d", &n);
	
	if (n == 2 || n == 6 || n == 7 || n == 8)
	{
		printf("Think before submission");
	}
	else
	{
		printf("Solve harder problems");
	}
	
	return 0;
}